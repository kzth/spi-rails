<h1>New user</h1>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.label :account_name, 'アカウント名' %>
    <%= f.text_field :account_name %><br />
    <%= f.label :pasword, 'パスワードの入力' %>
    <%= f.text_field :password, size: 25 %><br />
    <%= label_tag 'password_conf', 'パスワードの確認' %>
    <%= text_field_tag :password_conf, '', size: 25 %><br />
    <%= f.submit %>
  </div>
<% end %>
